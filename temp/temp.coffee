# index 
import house from './house'

dataHandler = (houses) =>
  result = xxxx houses
  result

importToLeanCloud = (dataHandler) =>
  {
    landlord
    tenants
    houses
    rooms
    beds
  } = result
  await app.dispatch.landlordSave landlord
  await ...

writeJsonFile = (dataHandler) =>
  jsFs.writeFileSync dataHandler, './xxxx.json'

export {
  dataHandler
  importToLeanCloud
  writeJsonFile
}

export default => dataHandler houses
